# Contracts

This directory is the future source of truth for cross-service HTTP and event contracts.

The skeleton stage intentionally defines no business schemas, event envelopes, generated clients, topics, or compatibility policy. Add contracts only when the corresponding service-boundary task is approved.
